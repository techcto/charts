# apiVersion: certmanager.k8s.io/v1alpha1
# kind: Certificate
# metadata:
#   name: {{ .Values.dns.zone.name }}            #name of certificate
# spec:
#   secretName: {{ .Values.dns.zone.name }}      #name of tls secret
#   issuerRef:
#     name: letsencrypt-prod
#     kind: ClusterIssuer
#   commonName: "*.{{ .Values.dns.zone.name }}"
#   dnsNames:
#   - {{ .Values.dns.zone.name }}
#   acme:
#     config:
#     - dns01:
#         provider: route53
#       domains:
#       - "*.{{ .Values.dns.zone.name }}"
#       - {{ .Values.dns.zone.name }}